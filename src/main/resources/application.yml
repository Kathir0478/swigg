jwt:
  secret: your-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm
  access:
    expiration: 3600000
  refresh:
    expiration: 604800000

google:
  maps:
    api-key: ${GOOGLE_MAPS_API_KEY:your-google-maps-api-key-here}

file:
  upload-dir: ./uploads/restaurant-images

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
